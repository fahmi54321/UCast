<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.ucast">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name=".Di.UCastApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.UCast">
        <activity android:name=".View.loginpage.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- IntroSlider Activity -->
        <activity
            android:name=".View.IntroSlider.IntroSliderActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen"></activity>
        <!-- IntroSlider Activity -->
        <activity android:name=".View.Customers.DetailsCustomerActivity"></activity>

        <activity
            android:name=".View.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>
        <activity android:name=".View.HomeActivity">

        </activity>
        <activity android:name=".View.MainActivity" />
    </application>

</manifest>